```
将A转换为3*3的矩阵
np.asarray(A).reshape(size*3,3) 
A、B相乘
np.dot(A,B) 
求A的逆
np.linalg.pinv(A)
求A的逆协方差矩阵
np.linalg.inv(A)
将A开方
np.sqrt(A) 
A,B相加
np.add(A,B)
将A的每一位都除以2
np.divide(A,2)
A-B
np.subtract(A,B)
```
